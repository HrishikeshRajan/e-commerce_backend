export interface imageUrl {
  publicId: string
  secureUrl: string
  url: string
}

export interface CloudinaryApi {
  id: string
  secure_url: string
}

export interface modifiedImageUrl {
  url: string
  secure_url: string
}
